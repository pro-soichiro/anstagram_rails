<h1><%= @department.name %>へのメンバー追加</h1>

<%= form_with model: @department, url: users_department_url do |form| %>
  <%= form.collection_check_boxes(:user_ids, User.all, :id, :full_name)  %>
  <%= form.submit %>
<% end %>

<%= link_to @department.name + "へ戻る", @department %>