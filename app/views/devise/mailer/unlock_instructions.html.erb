<p><%= @resource.last_name %>さん、こんにちは！</p>

<p>サインインに何度も失敗したため、アカウントがロックされました。</p>

<p>下のリンクをクリックして、アカウントのロックを解除してください:</p>

<p><%= link_to 'アカウントのロックを解除', unlock_url(@resource, unlock_token: @token) %></p>
