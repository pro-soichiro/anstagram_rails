<% if current_user.likes_posts.exists?(post.id) %>
  <%= link_to 'unlike', like_path(post.id), data: { turbo_method: :delete } %>
<% else %>
  <%= link_to 'like', likes_path(id: post.id), data: { turbo_method: :post } %>
<% end %>